server:
  port: 8888

spring:
    profiles:
        active: git

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pramod-jazz/apps-platform-acceleration-spring-configuration-code.git